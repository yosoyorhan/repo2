name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    env:
      # Supabase public (anon) runtime config baked at build time
      VITE_SUPABASE_URL: https://djxukpbhlbomtvxejxtl.supabase.co
      VITE_SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRqeHVrcGJobGJvbXR2eGVqeHRsIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjA1OTg2MTIsImV4cCI6MjA3NjE3NDYxMn0.IGNiKLwsqbXetRFRLVuyvJQSzi3G5zzyGG8OOzRcjJY
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build (GitHub Pages base path)
        run: |
          export GITHUB_PAGES=true
          npm run build
          cp dist/index.html dist/404.html

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - name: Deploy to GitHub Pages
        id: deploy
        uses: actions/deploy-pages@v4

    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
